---
description: "INTEG — element-first portal wire, vertical slices (только при INTEG-командах)"
alwaysApply: false
---
# INTEGR — общий контекст

## ELEMENT-FIRST

Один UI-элемент за сессию IMPLEMENT. `INTEG PLAN` → registry · `DECOMPOSE` → eNN+§Contract · `IMPLEMENT` → BACK+FRONT wire+§0.11 · `QA` → journey E2E.

**Запрещено:** mock forever без §Contract; layer-first нарезка; raw SQL — только QueryBuilder.

## SCENARIO TESTS

User-visible flow → scenario test до FINISH; QA прогон + дыры. Playwright (UI) · pytest HTTP outcome (контракт) · vitest alone **недостаточно** для «пользователь нажал X». **FORBIDDEN Handoff:** «manual smoke only» без scenario path.

## pytest runner (HARD)

cwd = корень репо; `.venv/bin/pytest …`; **FORBIDDEN** голый `pytest`.

## QUERY BUILDER

`@.agents/skills/query-builder/SKILL.md` — `HTTP ?filters= → PaginateQueryParams → QueryBuilder → crud.get_multi`; `relation__column` → join; числа → `gt`/`lt`.

## КОНТРАКТ

§Contract в `decompose-*/eNN-*.md`: method+path · request · response · DB. Skill TDD: только из workflow.

## BACK / FRONT в одном eNN

BACK если §API=❌: schema, endpoint, mapping_filters, targeted pytest. FRONT: api client, filter-utils, wire, vitest, убрать mock. Wire: §0.11 endpoint↔fetch.

## MEMORY BANK

- `activeContext` → `load_now` first
- PLAN: portal implement + routes grep; IMPLEMENT: ONE `eNN-*.md`
- Не читать as-built BACK/FRONT plan/decompose; gap — только через GAP CLOSE

## ПЕРЕХОДЫ

```
INTEG IMPLEMENT eNN → (Gaps) INTEG GAP → BACK/FRONT → INTEG GAP CLOSE @implement
INTEG PLAN → DECOMPOSE → IMPLEMENT eNN+1 → QA → REFLECT → ARCHIVE NOW
```

## ФОРМАТ

`OK INTEG <РЕЖИМ> - [действие]`
