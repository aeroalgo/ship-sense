---
description: "INTEG QA gates — contract + scenario journeys (lean)"
alwaysApply: false
---
# INTEG QA — gates

**Артефакт:** `memory-bank/integration/qa/qa-YYYYMMDD-<slug>.md`

## Gates

1. Lean load §7: ONE eNN §Contract + implement + diff
2. §0.11 + INTEG-CHECKLIST; contract drift = FAIL
3. Tests: `.venv/bin/pytest` из корня + vitest + scenario (Playwright); FRONT runners **parent only**
4. P0/P1 user-visible без scenario → **write** before PASS
5. **fail|blocked:** `review.md` + Handoff — `finish-doc-router.md`

## Abort

- PASS без scenario · subagent runs vitest/playwright · голый `pytest` → reject/FAIL
