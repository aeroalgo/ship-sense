---
description: "Режим INTEG IMPLEMENT — wire одного UI-элемента back+front"
alwaysApply: false
---
### Команда: INTEG IMPLEMENT
**Контекст**: @.cursor/rules/integration_developer/mainrule-core.mdc
**Gates**: @.cursor/rules/integration_developer/isolation_rules/_lean/implement.mdc
**Скиллы:** query-builder · tdd · fastapi-templates · frontend-patterns · frontend-testing · vercel-react-best-practices · verification-before-completion

**Цель:** Один UI-элемент — live API, без mock в prod path.

**Процесс**:
1. `OK INTEG IMPLEMENT - Wire UI-элемента` → **Gates** · graphify
2. `load_now` + ONE `decompose-*/eNN-*.yaml`; hub `implement-<plan_id>/index.md` если нет  
   **AC = eNN (§Contract).** **FORBIDDEN** полный `plan-*.md` / back|front plan в `load_now`. Jump `plan §N` только при нехватке.
3. **Contract gate:** §Contract из шага — пуст → допиши до кода

## Фазы A–F

**A** Discovery: §UI/API/Data/DB; grep mock; §Gaps  
**B** BACK (если §BACK): schema → endpoint → targeted `.venv/bin/pytest`  
**C** FRONT: api client → filter-utils → vitest → убрать mock  
**D** Wire: fetch + loading/error + URL sync  
**E** §0.11 + mock удалён  
**E2** Scenario: user-visible → Playwright **или** pytest HTTP outcome  
**F** FINISH: finish-block — **порядок:** Write step (`epic-step.yaml`) → `validate-step` exit 0 → Handoff → finalize `result.yaml` → `@verify`; epic complete → §Handoff epic complete  
Next-mode: только `loop/transitions.yaml` (+ runner; в т.ч. `gaps_found` → GAP) · @.cursor/rules/shared/workflow-loop-state.mdc

## Step format (HARD — loop after-hook)

Шаблон: `@.cursor/templates/implement/epic-step.yaml`. Канон = `epic_lib.validate_implement_step_format` = `@verify` §6.

| Обязательно | Значение |
|-------------|----------|
| yaml keys | `plan_id` · `element_ref` · `grep_control` · `verification_results` · `gaps` · `checkpoints[]` · `status: completed` |

Самопроверка перед finalize: `python3 .claude/hooks/epic_resolve.py validate-step --path memory-bank/integration/implement/implement-<plan>/eNN-<slug>.yaml`

**FORBIDDEN:** `.md` shard для eNN · BACK-секции вместо integ-обязательных · `## Handoff` в step.

**Loop repair:** при format FAIL runner даёт до **3×** RESULT REPAIR (`prepare-repair`) — только правка step/result, без следующего eNN.

**Антипаттерны:** query-builder/references/ANTI-PATTERNS.md

**Верификация**: gates + §0.11 + FINISH + graphify
