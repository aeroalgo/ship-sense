---
description: "Режим INTEG QA — контракт back↔front"
alwaysApply: false
---
### Команда: INTEG QA
**Контекст**: @.cursor/rules/integration_developer/mainrule-core.mdc
**Gates**: @.cursor/rules/integration_developer/isolation_rules/_lean/qa.mdc
**Скиллы:** query-builder · verification-before-completion · frontend-testing · python-testing-patterns · playwright-best-practices · playwright-generate-test

**Процесс**:
1. `OK INTEG QA - Верификация интеграции` → **Gates**
2. §Contract + §0.11 + INTEG-CHECKLIST; tests — gates (`.venv/bin/pytest` из корня)
3. `memory-bank/integration/qa/qa-YYYYMMDD-<slug>.md`
4. fail|blocked → §Fix plan (`INTEG|BACK|FRONT BUGFIX <subject>`)
5. **FINISH:** finish-block (`code_changed: no`|`yes` если дописали тесты) + Handoff  
   Next-mode: только `loop/transitions.yaml` (+ runner из `result.yaml`) · @.cursor/rules/shared/workflow-loop-state.mdc  
   **FORBIDDEN:** копировать цепочки pass/blocked/fail → … здесь  
   @.cursor/rules/shared/workflow-loop-state.mdc

**Верификация**: gates + contract + scenario + Handoff
