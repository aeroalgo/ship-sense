---
description: "Режим FRONT DECOMPOSE — план → атомарные шаги sNN"
alwaysApply: false
---
### Команда: FRONT DECOMPOSE
**Контекст**: @.cursor/rules/front_developer/mainrule-core.mdc
**Gates**: @.cursor/rules/front_developer/isolation_rules/_lean/decompose.mdc
**Situational skills:** @.cursor/rules/front_developer/skills-gate-situational.mdc (allowlist + checklist + Design Core)
**Скиллы (сессия DECOMPOSE):** @.agents/skills/writing-plans/SKILL.md; @.agents/skills/brainstorming/SKILL.md (если размыто)  
**Не грузить:** `executing-plans`, `breakdown-plan`

**Цель:** `plan-<id>.md` → `memory-bank/front/plan/decompose-<plan_id>/` (`index.md` + `sNN-*.yaml`). НЕ писать код.  
**Вход:** `load_now` или `FRONT DECOMPOSE plan-<id>` (в одном сообщении).

---

## Skills gate → каждый `sNN` (HARD)

Канон для IMPLEMENT — **только** списки в step. IMPLEMENT **не** угадывает скиллы и **не** держит широкий слой A.

Каждый FRONT `sNN`: `**visible_ui:**` + **Impl skills** = **Impl Core ∪ situational** (+ **Design Core** пакет если `yes`).

### Impl Core (всегда — ровно эти 6)

- `.agents/skills/tdd/SKILL.md`
- `.agents/skills/frontend-testing/SKILL.md`
- `.agents/skills/frontend-patterns/SKILL.md`
- `.agents/skills/next-best-practices/SKILL.md`
- `.agents/skills/vercel-react-best-practices/SKILL.md`
- `.agents/skills/vercel-composition-patterns/SKILL.md`

### Situational + Design

1. **Read** @.cursor/rules/front_developer/skills-gate-situational.mdc
2. Прогони **Checklist — DECOMPOSE** (`visible_ui`, playwright, …)
3. `visible_ui: yes` → скопировать **Design Core** (все 6) в блок Design skills  
   `visible_ui: no` → `**Design skills:** —` + причина
4. Cap situational ≤ **4** (+ 6 Impl Core; Design Core не в cap situational)
5. В step — только итоговые пути. **FORBIDDEN:** скиллы вне allowlist/пакетов; копировать весь checklist в `sNN`

**Сессия DECOMPOSE:** если ≥1 шаг с `visible_ui: yes` — **Read Design Core один раз** до нарезки этих `sNN`.

**FINISH-only** (не в `sNN`): `verification-before-completion` · `requesting-code-review`

**Index:** «per-step skills gate (Impl Core + situational + Design Core); канон в каждом sNN».

IMPLEMENT: Read **только** Impl + Design из step (+ FINISH-only). См. `workflow-implement.mdc`.

---

**Процесс**:
1. `OK FRONT DECOMPOSE - [plan_id]`
1a. Прочитай **Gates** + **skills-gate-situational**
2. Прочитай план (L3–4: все shards)
2b. UI в scope → Read Design Core до нарезки `visible_ui: yes`
3. Skills note в `decompose-<plan_id>/index.md` (gate, не полный каталог)
4. Атомарные шаги: files, `step_id`, `needs_creative`, `visible_ui`, next (`creative`|`implement`), `tdd: true`
5. Каждый `sNN-<slug>.yaml` (@.cursor/templates/decompose/epic-step.yaml): skills gate + lean-интерфейсы (без полных interface/TSX), процесс, тест-кейсы, checkpoint
5a. **FAIL** без `visible_ui` / без Impl Core / `yes` без Design Core / `no` без причины / situational вне allowlist / situational >4
6. Папка из `.cursor/templates/decompose/`
7. Plan: index = единственный трекер; удали дубль-чеклисты sNN
7b. **FINISH:** finish-block (`code_changed: no`) — `load_now` → `s01-*.md`
8. `tasks.md` append-only
9. Next: CREATIVE или IMPLEMENT
10. **Новый чат** для IMPLEMENT

**Верификация**: gates + каждый `sNN` имеет `visible_ui` + Impl skills (+ Design block)
