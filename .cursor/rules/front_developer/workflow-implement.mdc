---
description: "Режим FRONT IMPLEMENT — UI по плану, TDD + Playwright targeted"
alwaysApply: false
---
### Команда: FRONT IMPLEMENT
**Контекст**: @.cursor/rules/front_developer/mainrule-core.mdc
**Gates**: @.cursor/rules/front_developer/isolation_rules/_lean/implement.mdc

**Скиллы — A∪B**

**A (always + design если visible_ui):** tdd · frontend-testing · playwright-best-practices · next-best-practices · vercel-react-best-practices · vercel-composition-patterns · frontend-patterns · playwright-generate-test · verification-before-completion · requesting-code-review; design stack (6) — если `visible_ui: yes` или JSX под `features/`|`components/`|`app/`. Pure lib / `visible_ui: no` — design skip.

**B:** Impl skills + Design skills из decompose `sNN` — Read до кода/UI. Union A∪B; **FORBIDDEN:** пропуск применимого A/B; design Read при `visible_ui: no`.

**Процесс**:
1. `OK FRONT IMPLEMENT - Начинаю реализацию` → **Gates**
2. `load_now` → ONE step; артефакт `implement-<plan_id>/sNN-<slug>.md`
3. Read A∪B до prod-edit; Design — до UI-кода
4. TDD targeted Vitest/RTL; scenario E2E если user-visible — **parent** runs
5. §0.11 · pre-FINISH skills
6. **FINISH:** finish-block (`code_changed: yes`); epic complete → §Handoff epic complete
7. → `FRONT IMPLEMENT sNN+1` **или** epic complete → `FRONT QA` (new chat)

**Верификация**: gates + FINISH/Handoff + graphify
