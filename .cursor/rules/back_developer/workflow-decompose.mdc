---
description: "Режим BACK DECOMPOSE — план → атомарные шаги"
alwaysApply: false
---
### Команда: BACK DECOMPOSE
**Контекст**: @.cursor/rules/back_developer/mainrule-core.mdc
**Gates**: @.cursor/rules/back_developer/isolation_rules/_lean/decompose.mdc
**Situational skills:** @.cursor/rules/back_developer/skills-gate-situational.mdc (allowlist + checklist DECOMPOSE)
**Скиллы (сессия DECOMPOSE):** @.agents/skills/writing-plans/SKILL.md; @.agents/skills/brainstorming/SKILL.md (если фаза размыта)  
**Не грузить:** `executing-plans`, `breakdown-plan`

**Цель:** `plan-<id>.md` → `decompose-<plan_id>/` (`index.md` + `sNN-*.yaml`). НЕ писать код.  
**Вход:** план из `load_now` или `BACK DECOMPOSE plan-<id>` (в одном сообщении).

---

## Skills gate → каждый `sNN` (HARD)

Канон для IMPLEMENT — **только** список в step. IMPLEMENT **не** угадывает скиллы и **не** держит широкий слой A.

Каждый `sNN`: `**code_surface:**` + блок **Impl skills** = **Core ∪ situational** (конкретные пути, без «(api) если…»).

### Core (всегда — ровно эти 4)

- `.agents/skills/tdd/SKILL.md`
- `.agents/skills/python-testing-patterns/SKILL.md`
- `.agents/skills/modern-python/SKILL.md`
- `.agents/skills/python-anti-patterns/SKILL.md`

### Situational

1. **Read** @.cursor/rules/back_developer/skills-gate-situational.mdc
2. Прогони **Checklist — DECOMPOSE** (yes → путь `.agents/skills/<skill>/SKILL.md`)
3. Cap: situational ≤ **6** (+ 4 core)
4. В step — только итоговые пути (bullet). **FORBIDDEN:** скиллы вне allowlist файла; копировать весь checklist в `sNN`

**FINISH-only** (не в `sNN`; только IMPLEMENT pre-FINISH):  
`verification-before-completion` · `requesting-code-review`

**Смешанный шаг:** один доминирующий `code_surface`; situational = union ответов checklist (в пределах cap).

**Index:** в `## Skills в контексте` — «per-step skills gate (Core + situational из skills-gate-situational); канон в каждом sNN».

IMPLEMENT: Read **только** Impl skills из step (+ FINISH-only). См. `workflow-implement.mdc`.

---

**Процесс**:
1. `OK BACK DECOMPOSE - [plan_id]`
1a. Прочитай **Gates** + **skills-gate-situational**
2. Прочитай план (L3–4: **все** shards)
3. Skills note в `decompose-<plan_id>/index.md` (gate, не полный каталог)
4. Атомарные шаги IMPLEMENT: files, `step_id`, `needs_creative`, `code_surface`, next phase (`creative`|`implement`), `tdd: true` по умолчанию
4a. Если шаг **требует** unresolved `CR-*` для корректной реализации, это должен быть **hard stop**: `needs_creative: yes (CR-...)`, `Next Phase: BACK CREATIVE`, ссылка на CR в `index.md`. Канон форм после close: `templates/decompose/epic-step.yaml` (`yes (CR-…) — **closed**`, не `no (… closed)` / не `yes (done)`). Упоминание CR только в тексте шага без `needs_creative` = FAIL
5. Каждый `sNN-<slug>.yaml` (@.cursor/templates/decompose/epic-step.yaml): **skills gate** (Core ∪ situational из checklist) + lean-интерфейсы (имена/поля/наследование, **без** тел и полных type), процесс словами, тест-кейсы, checkpoint. Не дублировать буллеты плана, не вставлять код
5a. **FAIL** если BACK-шаг без `**code_surface:**`, без Core-четырёх, с путями вне allowlist, с situational >6, или с «(api) если…» вместо конкретных путей
6. Папка `memory-bank/back/plan/decompose-<plan_id>/` из `@.cursor/templates/decompose/`
7. Обнови plan: ссылка на index как единственный трекер; удали дубль-чеклисты sNN; сохрани Goal/AC/архитектуру/risks
7b. **FINISH:** @.cursor/rules/shared/finish-block.mdc (`code_changed: no`) — `load_now` → `s01-*.md`, status `active`
8. `tasks.md` append-only
9. Next: `needs_creative` → CREATIVE; иначе IMPLEMENT
10. **Новый чат** для IMPLEMENT (context-session §2)

**Границы:** нет кода/тестов реализации; нет арх-решений вне плана (`needs_creative`); не путать с TASK.

**Верификация**: gates (все пункты)
