---
description: "Режим BACK BUGFIX — root cause, TDD, memory-bank/back/bugfix/"
alwaysApply: false
---
### Команда: BACK BUGFIX
**Контекст**: @.cursor/rules/back_developer/mainrule-core.mdc
**Gates**: @.cursor/rules/back_developer/isolation_rules/_lean/bugfix.mdc
**Скиллы:** systematic-debugging · diagnosing-bugs · tdd · python-testing-patterns · python-error-handling · verification-before-completion · requesting-code-review — пути `@.agents/skills/<name>/SKILL.md`

**Процесс**:
1. `OK BACK BUGFIX - [суть]` → **Gates** + @.cursor/rules/shared/epic-scoped-paths.mdc
2. **Вход из QA:** qa §Fix plan — одна строка → `BACK BUGFIX <subject>`; skills в `bugfix-YYYYMMDD-<slug>.md` под `bugfix/<epic_id>/` если эпик, иначе плоско в `bugfix/`
3. Reproduce → root cause (не mask) → regression red → fix → green (`.venv/bin/pytest` из корня)
4. §0.11 если refs · pre-FINISH skills
5. **FINISH:** finish-block (`code_changed: yes`); finalize `result.yaml` → runner ledger  
   Next-mode: только `loop/transitions.yaml` · @.cursor/rules/shared/workflow-loop-state.mdc · `finish-doc-router.md` §BUGFIX FINISH

**Верификация**: gates + root cause + FINISH + graphify
