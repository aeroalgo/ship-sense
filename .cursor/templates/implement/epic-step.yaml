schema: epic-implement/v1
role: back
step_id: sNN
plan_id: <plan_id>
task_id: T-xxx
title: sNN — <title> IMPLEMENT
level: L1
status: in_progress
decompose_ref: memory-bank/back/plan/decompose-<plan_id>/sNN-<slug>.yaml
implement_index: memory-bank/back/implement/implement-<plan_id>/index.md
date: YYYY-MM-DD

skills_used: []

done: []

files: []

# tests: HARD — только executable cmds (loop after запускает как shell).
# OK:  - '`.venv/bin/pytest path -q` — PASS'
# OK:  - '`npm exec tsc -- --noEmit`'
# FAIL: - 'npm exec tsc -- --noEmit — passed'  (prose без backticks)
# FAIL: - {command: …, result: …}
# PASS/counts → verification_results, не в tests.
tests: []

integration_check: []

checkpoints:
  - id: cp1
    criterion: from decompose checkpoints[0]
    status: pending

resume_from: cp1

# role: integ — дополнительно обязательны:
# element_ref: memory-bank/integration/plan/decompose-<plan_id>/eNN-<slug>.yaml
# gaps: { status: none }
# grep_control: [{ back: "...", front: "..." }]
# verification_results: ["..."]
# discovery: []
