schema: epic-qa/v1
role: back
date: '2026-07-30'
reviewer: BACK QA
verdict: blocked
scope:
- 'task: T-002'
- 'step: s01–s18 storage + semantic, с фокусом на s17/s18 integration wiring и storage
  tests'
- 'files: `apps/edge/storage/`, `apps/edge/semantic/`, `migrations/versions/`, `docker-compose.yml`,
  `tests/storage/`, `pyproject.toml'
checks:
- 'storage suite green: `PYTHONPATH=.:apps/edge/collector/src .venv/bin/pytest tests/storage/
  -q` — **65 passed in 0.58s**'
- 'backend suite без slow-тестов: `.venv/bin/pytest -m ''not slow'' -q` — **394 passed,
  3 deselected, 1 warning in 16.87s**'
- 'full suite collection: `.venv/bin/pytest --collect-only -q` — **397 tests collected**'
- 'Python syntax: `.venv/bin/python -m compileall -q apps/edge tests/storage` — pass'
- 'compose syntax: `docker compose config --quiet` — pass'
- 'live Compose state: `docker compose ps` — `db`, `writer`, `collector`, `emulator`
  healthy; writer image `shipsense/writer:dev`'
- 'live DB storage presence: `docker compose exec -T db psql ... ''\dt''` — `samples`,
  `events`, semantic/quarantine/quota/health tables present'
- 'live DB data sanity: `samples=75352`, `events=1929`, Alembic `006_compression_retention`'
- 'storage integration refs inspected (§0.11): compose, writer exports, database dependency
  and test wiring are internally consistent'
- storage edge cases covered by contract, correlation, quota, quarantine and load
  tests
- full repository suite including slow tests green
- 'lint/types/security tools green: `ruff`, `mypy`, `semgrep`, `bandit` unavailable
  in `.venv`'
issues:
- id: QA-1
  sev: blocker
  file: '`apps/edge/collector/tests/integration/test_mqtt_e2e.py`'
  msg: Slow/full suite did not reach a terminal result within the 180-second QA budget;
    execution stopped while running `test_mosquitto_mqtt_collector_emits_sample_and_lifecycle_event`.
    Поэтому полный suite не объявляется green.
- id: QA-2
  sev: high
  file: '`docker-compose.yml` / live logs'
  msg: 'Live services healthy, но `docker compose logs` содержит повторяющиеся `ModbusException`
    для poll groups и DB warnings/errors: compression policy already exists и запрос
    отсутствующей `timescaledb_information.jobs.last_run_started_at`. Live storage
    acceptance частично подтверждена counts/health, но runtime logs не чистые.'
- id: QA-3
  sev: medium
  file: '`.venv`'
  msg: '`ruff`, `mypy`, `semgrep`, `bandit` отсутствуют; lint/type/security gates
    этим инструментарием не выполнены.'
- id: QA-4
  sev: low
  file: '`.cursor/rules/back_developer/isolation_rules/_lean/creative.mdc`, `.kilo/instructions/spawn-hard.md`'
  msg: '`git diff --check` в рабочем дереве сообщает trailing whitespace в изменениях
    workflow-инструкций; это не storage runtime regression.'
blockers:
- Нельзя объявить BACK QA PASS, пока slow/full pytest suite не получает terminal green
  result либо не введён явный CI/runtime contract с отдельным smoke acceptance.
- Live Compose writer/DB/collector подняты и данные записываются, но повторяющиеся
  runtime errors/warnings требуют отдельного расследования до безусловного acceptance.
- Статические lint/type/security gates остаются непроверенными из-за отсутствующих
  инструментов.
fix_plan: []
limitations: []
suite:
- 'storage suite green: `PYTHONPATH=.:apps/edge/collector/src .venv/bin/pytest tests/storage/
  -q` — **65 passed in 0.58s**'
- 'backend suite без slow-тестов: `.venv/bin/pytest -m ''not slow'' -q` — **394 passed,
  3 deselected, 1 warning in 16.87s**'
- 'full suite collection: `.venv/bin/pytest --collect-only -q` — **397 tests collected**'
plan_id: v1-p1-storage
epic_id: v1-p1-storage
