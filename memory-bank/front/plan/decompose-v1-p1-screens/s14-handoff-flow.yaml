schema: epic-decompose/v1
role: front
step_id: s14
plan_id: v1-p1-screens
title: Handoff flow 6→5/1
next_phase: FRONT IMPLEMENT
needs_creative: no | **tdd:** yes
goal: 'Пересменочный UX: с Watch «Что активно сейчас» → overview/journal; session events visible in journal
  filters.'
context:
  consumes:
  - '**Consumes:** s08 session, s09–s10, s13 watch'
  - '**Produces:** handoff CTAs + routing helpers + journal session filter chip'
  files:
  - frontend/src/features/watch/HandoffButton.tsx (Создание)
  - frontend/src/lib/routing/handoff.ts (Создание)
  - frontend/src/features/journal/SessionEventFilter.tsx (Создание) — опц.
  - frontend/src/features/watch/HandoffButton.test.tsx (Создание)
  - Modify WatchPage / JournalPage as needed
as_built:
- 'Consumes: s08 session, s09–s10, s13 watch'
delta:
- 'Создание: frontend/src/features/watch/HandoffButton.tsx'
- 'Создание: frontend/src/lib/routing/handoff.ts'
- 'Создание: frontend/src/features/journal/SessionEventFilter.tsx'
- 'Создание: frontend/src/features/watch/HandoffButton.test.tsx'
- Modify WatchPage / JournalPage as needed
out_of_scope:
- Полный test suite / QA epic — не этот sNN
- Соседние sNN из decompose index — не трогать
- Полный Playwright PW-01..N → отдельный e2e шаг / QA
- INTEG wire contract — не этот FRONT шаг
checkpoints:
- id: cp1
  criterion: 'Код шага на месте: frontend/src/features/watch/HandoffButton.tsx, frontend/src/lib/routing/handoff.ts…'
  verify: cd frontend && npm exec vitest -- run src/features/watch/HandoffButton.test.tsx
- id: cp2
  criterion: Targeted tests PASS по AC шага
  verify: cd frontend && npm exec vitest -- run src/features/watch/HandoffButton.test.tsx --reporter=dot
verify:
- cd frontend && npm exec vitest -- run src/features/watch/HandoffButton.test.tsx
- cd frontend && npm exec vitest -- run src/features/watch/HandoffButton.test.tsx
tdd:
- '1. Red: targeted failing test из checkpoints.verify'
- '2. Green: минимальная реализация delta'
- 3. Wire + §Verify
