schema: epic-decompose/v1
role: integ
step_id: e08
plan_id: v1-portal
title: Overview assets tree + aggregate status
element_id: e08
next_phase: INTEG IMPLEMENT
goal: hierarchical plant→system→equipment→tag cards with aggregate status, last_value/quality
ui:
  route: '** `/overview` → OverviewPage'
  components: '** DS `AggregateShipStatus`, `OverviewGroupCard`, lamps via tree'
  user_sees: '** hierarchical plant→system→equipment→tag cards with aggregate status,
    last_value/quality'
data_need:
- hierarchical plant→system→equipment→tag with aggregate status, last_value/quality
api_today:
- status: ✅
  detail: '`GET /api/assets/tree` Cache-Control max-age=60'
- status: ⚠️
  detail: MSW `assetsTreeFixture`
- status: —
  detail: 'FRONT: `useAssetsTree` → `fetchAssetsTree`'
contract:
  method_path: '** `GET /api/assets/tree`'
  query_keys: '** none'
  response_shape: '** `{ root: AssetTreeNode, generated_at }`'
  front_client: '** `lib/api/assets.ts` → `fetchAssetsTree`'
db: '- assets semantic + latest values (Timescale)'
back:
- already live (T-003 s02)
- 'Realtime patch: `useOverviewRealtime` merges WS values into React Query cache —
  contract tag_id identity'
front:
- '`useAssetsTree` already wired (FRONT s09)'
- '`treeUtils` must tolerate missing optional fields'
as_built:
- GET /api/assets/tree live (T-003 s02)
- frontend/src/lib/api/assets.ts → fetchAssetsTree
- useAssetsTree wired (FRONT s09)
delta:
- frontend/src/lib/api/assets.ts + MSW assetsTreeFixture align with live shape
- frontend treeUtils tolerate missing optional fields
- useOverviewRealtime merges WS values — tag_id identity
out_of_scope:
- Полный INTEG QA journey / соседние eNN — не этот шаг
- Не расширять scope за пределы contract + checkpoints
grep_control:
- back: GET /api/assets/tree
  front: fetchAssetsTree
verify:
- rg -n "/api/assets/tree|fetchAssetsTree" frontend/src apps/api
- cd frontend && npm exec vitest -- run src/features/overview/OverviewPage.test.tsx
- .venv/bin/pytest apps/api/tests/api/test_assets_tree.py -q
tdd:
- '1. Red: vitest OverviewPage with tree fixture → assert cards'
- '2. Green: live fetch, remove MSW'
- 3. Wire + §Verify
checkpoints:
- id: cp1
  criterion: /overview shows hierarchical tree from DB
  verify: rg -n "/api/assets/tree|fetchAssetsTree" frontend/src apps/api
- id: cp2
  criterion: Cards display last_value + status
  verify: cd frontend && npm exec vitest -- run src/features/overview/OverviewPage.test.tsx
- id: cp3
  criterion: Realtime WS updates tree in place
  verify: .venv/bin/pytest apps/api/tests/api/test_assets_tree.py -q
