schema: epic-decompose/v1
role: integ
step_id: e29
plan_id: v1-portal
title: Dev appearance (non-prod)
element_id: e29
next_phase: INTEG IMPLEMENT
goal: theme/design controls (tokens, variants)
ui:
  route: '** `/dev/appearance` → AppearanceControls'
  components: '** `AppearanceControls`'
  user_sees: '** theme/design controls (tokens, variants)'
data_need:
- none (static, non-prod)
api_today:
- status: —
  detail: static, no API
contract:
  method_path: '** N/A'
  response_shape: '** N/A'
db: '- none'
back:
- n/a
front:
- '`/dev/appearance` route + `AppearanceControls` already wired (FRONT s02)'
- Exclude from production nav; no API mock required
as_built:
- 'Contract: N/A'
delta:
- 'Outcome: theme/design controls (tokens, variants)'
- 'BACK caveat: n/a'
- 'FRONT: Exclude from production nav; no API mock required'
- 'Data need: none (static, non-prod)'
- 'FRONT: Exclude from production nav; no API mock required'
- 'BACK: n/a'
out_of_scope:
- Полный INTEG QA journey / соседние eNN — не этот шаг
- Не расширять scope за пределы contract + checkpoints
grep_control:
- back: N/A
  front: /dev/appearance static
verify:
- rg -n '/dev/appearance|AppearanceControls' frontend/src
- cd frontend && npm exec vitest -- run src/features/session --passWithNoTests
- rg -n 'AppearanceControls' frontend/src/features/shell frontend/src/app
tdd:
- '1. Red: n/a (static)'
- '2. Green: page renders controls'
- 3. Wire + §Verify
checkpoints:
- id: cp1
  criterion: /dev/appearance renders in dev only
  verify: rg -n '/dev/appearance|AppearanceControls' frontend/src
- id: cp2
  criterion: Theme/design controls visible
  verify: cd frontend && npm exec vitest -- run src/features/session --passWithNoTests
- id: cp3
  criterion: Not in production nav
  verify: rg -n 'AppearanceControls' frontend/src/features/shell frontend/src/app
