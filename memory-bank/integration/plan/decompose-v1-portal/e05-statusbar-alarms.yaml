schema: epic-decompose/v1
role: integ
step_id: e05
plan_id: v1-portal
title: StatusBar alarms
element_id: e05
next_phase: INTEG IMPLEMENT
goal: alarm/warning chips in top bar; click → /journal?asset_id=&from=
ui:
  route: '** `(auth)/*` (inside `AppShell`)'
  components: '** DS `StatusBar` in `AppShell`; hook `features/shell/useStatusBarAlarms.ts`'
  user_sees: '** alarm/warning chips in top bar; click → `/journal?asset_id=&from=`'
data_need:
- 'bootstrap: recent alarm/warning events'
- 'live: WS events channel updates'
- click → `/journal?asset_id=&from=`
api_today:
- status: ✅
  detail: REST `GET /api/events` (bootstrap query in hook)
- status: ✅
  detail: WS `/api/stream` subscribe events
- status: ⚠️
  detail: MSW covers REST events only (не WS)
contract:
  method_path: '** `GET /api/events?severity=alarm&severity=warning&limit=…`'
  query_keys: '** `severity[]`, `limit`'
  response_shape: '** `{ items: EventItem[], next_cursor, has_more }`'
  response_header: '** `X-Events-Reconstruction: edge_only|…`'
  front_client: '** `lib/api/events.ts` → `fetchEvents`; `useWsChannel`'
db: '- events store (Timescale)'
back:
- already live (T-003 s04/s06)
- severity filter must match BACK enum (`info|warning|alarm`)
front:
- '`useStatusBarAlarms` already wired (FRONT s07)'
- Deep-link query keys must match journal filters (`asset_id`, `from`)
as_built:
- 'API live: REST `GET /api/events` (bootstrap query in hook)'
- 'API live: WS `/api/stream` subscribe events'
- 'BACK: already live (T-003 s04/s06)'
- 'FRONT: useStatusBarAlarms already wired (FRONT s07)'
- 'UI seams: DS `StatusBar` in `AppShell`; hook features/shell/useStatusBarAlarms.ts'
delta:
- 'Исправить/заменить mock: MSW covers REST events only (не WS)'
- 'BACK caveat: severity filter must match BACK enum (`info|warning|alarm`)'
- 'FRONT: Deep-link query keys must match journal filters (`asset_id`, `from`)'
- 'Data need: bootstrap: recent alarm/warning events'
- 'Data need: live: WS events channel updates'
- 'Data need: click → /journal?asset_id=&from='
out_of_scope:
- Полный INTEG QA journey / соседние eNN — не этот шаг
- Не расширять scope за пределы contract + checkpoints
grep_control:
- back: GET /api/events + WS events
  front: fetchEvents` + `useWsChannel
verify:
- rg "/api/events\|/api/stream" frontend/src api/
- cd frontend && npm exec vitest -- run src/**/StatusBar.alarms.test.tsx
- rg -n 'v3' frontend/src frontend/e2e
tdd:
- '1. Red: vitest StatusBar with alarm fixture → assert chips'
- '2. Green: live fetch + WS, remove MSW for REST'
- 3. Wire + §Verify
checkpoints:
- id: cp1
  criterion: StatusBar shows recent alarms/warnings
  verify: rg "/api/events\|/api/stream" frontend/src api/
- id: cp2
  criterion: Click → journal with filter applied
  verify: cd frontend && npm exec vitest -- run src/**/StatusBar.alarms.test.tsx
- id: cp3
  criterion: Live WS appends new alarm
  verify: rg -n 'cp3' frontend/src frontend/e2e
