schema: epic-decompose/v1
role: integ
step_id: e28
plan_id: v1-portal
title: Admin ops (storage / OTA / audit)
element_id: e28
next_phase: INTEG IMPLEMENT
goal: (future) storage health, OTA status/approve/trigger, access audit list
ui:
  route: '** none in crew portal; possible `/admin` later'
  components: '** (future) admin panels (role-gated)'
  user_sees: '** (future) storage health, OTA status/approve/trigger, access audit list'
data_need:
- storage health, OTA status/approve/trigger, access audit list
api_today:
- status: ✅
  detail: '`GET /api/admin/storage`'
- status: ✅
  detail: GET/POST /api/admin/ota/status
- status: ✅
  detail: '`GET /api/admin/access/audit?limit&offset`'
- status: —
  detail: FRONT ❌
contract:
  method_path: '** `GET /api/admin/access/audit?limit&offset`'
  response_shape: '** `{ items: [...], has_more }`'
  front_client: '** (future) `lib/api/admin.ts` — role-gated'
db: '- admin ops tables (storage, ota, access_audit)'
back:
- already live (T-005 s14/s16)
- Out of P0 crew journeys; P2 section portal or separate ops app
- 'If in scope: new routes under authenticated admin role only'
front:
- no UI yet (crew portal)
- No accidental public fetch from crew screens
as_built:
- 'API live: GET /api/admin/storage'
- 'API live: GET/POST /api/admin/ota/status'
- 'API live: GET /api/admin/access/audit?limit&offset'
- 'BACK: already live (T-005 s14/s16)'
- 'UI seams: (future) admin panels (role-gated)'
delta:
- 'Довести client/fixture: FRONT ❌'
- 'BACK caveat: Out of P0 crew journeys; P2 section portal or separate ops app'
- 'BACK caveat: If in scope: new routes under authenticated admin role only'
- 'FRONT: no UI yet (crew portal)'
- 'FRONT: No accidental public fetch from crew screens'
- 'Data need: storage health, OTA status/approve/trigger, access audit list'
out_of_scope:
- Полный INTEG QA journey / соседние eNN — не этот шаг
- Не расширять scope за пределы contract + checkpoints
grep_control:
- back: GET/POST /api/admin/*
  front: (future client, role-gated)
verify:
- '`grep -r "/api/admin" frontend/src api/`'
- pytest admin authz
- no accidental public fetch from crew screens
tdd:
- '1. Red: n/a (P2, no UI yet)'
- '2. Green: role-gated admin UI (after decision)'
- 3. Wire + §Verify
checkpoints:
- id: cp1
  criterion: Admin routes role-gated (Permission.*)
  verify: grep -r "/api/admin" frontend/src api/
- id: cp2
  criterion: No crew screen accidentally calls admin API
  verify: pytest admin authz
- id: cp3
  criterion: Audit list paginates
  verify: no accidental public fetch from crew screens
