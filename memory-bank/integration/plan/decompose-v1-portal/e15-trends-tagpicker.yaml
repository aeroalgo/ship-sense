schema: epic-decompose/v1
role: integ
step_id: e15
plan_id: v1-portal
title: Trends TagPicker
element_id: e15
next_phase: INTEG IMPLEMENT
goal: selectable tags from assets tree (leaves only)
ui:
  route: '** `/trends` → `TrendsPage` + DS TagPicker'
  components: '** `TagPicker`'
  user_sees: '** selectable tags from assets tree (leaves only)'
data_need:
- tag leaves from assets tree (id, name, unit)
api_today:
- status: ✅
  detail: '`GET /api/assets/tree` (reuse e08 client)'
- status: —
  detail: 'FRONT: flatten `kind==="tag"`'
contract:
  front_client: '** `lib/api/assets.ts` → `fetchAssetsTree` (reuse)'
db: '- none (derived from assets tree)'
back:
- already live (e08, T-003 s02)
front:
- '`TagPicker` already wired (FRONT s12)'
- Tag id string identity must match series `tag` query param
- Empty tree → StateShell empty (not spinner forever)
as_built:
- 'FRONT client: lib/api/assets.ts` → `fetchAssetsTree` (reuse)'
- 'BACK: already live (e08, T-003 s02)'
delta:
- 'Outcome: selectable tags from assets tree (leaves only)'
- 'FRONT: Tag id string identity must match series `tag` query param'
- 'FRONT: Empty tree → StateShell empty (not spinner forever)'
- 'Data need: tag leaves from assets tree (id, name, unit)'
- 'FRONT: Tag id string identity must match series `tag` query param'
- 'FRONT: Empty tree → StateShell empty (not spinner forever)'
out_of_scope:
- Полный INTEG QA journey / соседние eNN — не этот шаг
- Не расширять scope за пределы contract + checkpoints
grep_control:
- back: GET /api/assets/tree (kind=tag)
  front: fetchAssetsTree + filter
verify:
- rg "TagPicker" frontend/src
- cd frontend && npm exec vitest -- run src/features/trends
- rg -n 'cp3' frontend/src frontend/e2e
tdd:
- '1. Red: vitest TagPicker with tree fixture'
- '2. Green: live tree, flatten tags'
- 3. Wire + §Verify
checkpoints:
- id: cp1
  criterion: TagPicker shows all tag leaves from tree
  verify: rg "TagPicker" frontend/src
- id: cp2
  criterion: Select tag → triggers series fetch
  verify: cd frontend && npm exec vitest -- run src/features/trends
- id: cp3
  criterion: Empty tree shows empty state
  verify: rg -n 'cp3' frontend/src frontend/e2e
