schema: epic-decompose/v1
role: integ
step_id: e12
plan_id: v1-portal
title: Reconstruction banner
element_id: e12
next_phase: INTEG IMPLEMENT
goal: banner text from `X-Events-Reconstruction` header (e.g., "edge_only")
ui:
  route: '** /journal'
  components: '** `ReconstructionBanner` reads response header from events fetch'
  user_sees: '** banner text from `X-Events-Reconstruction` header (e.g., "edge_only")'
data_need:
- '`X-Events-Reconstruction` value (`edge_only` etc.)'
api_today:
- status: ✅
  detail: BACK sets header; FRONT constant `EVENTS_RECONSTRUCTION_HEADER`
- status: ⚠️
  detail: MSW sets header
contract:
  response_header: '** `X-Events-Reconstruction: <enum>`'
  front_client: '** read header from `fetchEvents` response'
db: '- none (header from events endpoint)'
back:
- already live (T-003 s04)
- CORS expose header обязателен для browser (compose API CORS config)
- Banner copy не маскирует partial reconstruction
front:
- '`ReconstructionBanner` already wired (FRONT s10)'
- Read header from response, not invent value
as_built:
- 'FRONT client: read header from `fetchEvents` response'
- 'BACK: already live (T-003 s04)'
delta:
- 'Outcome: banner text from `X-Events-Reconstruction` header (e.g., "edge_only")'
- 'Исправить/заменить mock: MSW sets header'
- 'BACK caveat: CORS expose header обязателен для browser (compose API CORS config)'
- 'BACK caveat: Banner copy не маскирует partial reconstruction'
- 'FRONT: Read header from response, not invent value'
- 'Data need: X-Events-Reconstruction` value (`edge_only etc.)'
- 'FRONT: Read header from response, not invent value'
- 'BACK: CORS expose header обязателен для browser (compose API CORS config)'
out_of_scope:
- Полный INTEG QA journey / соседние eNN — не этот шаг
- Не расширять scope за пределы contract + checkpoints
grep_control:
- back: header X-Events-Reconstruction
  front: EVENTS_RECONSTRUCTION_HEADER constant
verify:
- rg "X-Events-Reconstruction\|EVENTS_RECONSTRUCTION" frontend/src api/
- rg -n 'e12' frontend/e2e frontend/src
- rg -n 'e12-v3' frontend/src apps || rg -n 'v3' frontend/src
tdd:
- '1. Red: vitest banner with header fixture'
- '2. Green: live header from API, remove MSW'
- 3. Wire + §Verify
checkpoints:
- id: cp1
  criterion: Banner shows when header present
  verify: rg "X-Events-Reconstruction\|EVENTS_RECONSTRUCTION" frontend/src api/
- id: cp2
  criterion: Header value matches BACK enum
  verify: rg -n 'e12' frontend/e2e frontend/src
- id: cp3
  criterion: No banner when header absent
  verify: rg -n 'e12-cp3' frontend/src apps || rg -n 'cp3' frontend/src
