schema: epic-decompose/v1
role: integ
step_id: e11
plan_id: v1-portal
title: Journal filters + infinite list
element_id: e11
next_phase: INTEG IMPLEMENT
goal: filter bar + infinite-scroll list of events
ui:
  route: '** `/journal` → JournalPage'
  components: '** DS `EventFilters`, `EventRow`; hooks: `useEventsInfinite`, `journalFilters.ts`,
    `journalSort.ts`'
  user_sees: '** filter bar + infinite-scroll list of events'
data_need:
- 'cursor pagination; filters: from/to, event_name[], severity[], asset_id, source,
  ack?, limit'
api_today:
- status: ✅
  detail: '`GET /api/events`'
- status: ⚠️
  detail: MSW (static list; cursor poorly simulated)
- status: —
  detail: 'FRONT: `lib/api/events.ts` `fetchEvents`'
contract:
  method_path: '** `GET /api/events`'
  query_keys: '** `from?`, `to?`, `event_name[]?`, `severity[]?`, `asset_id?`, `source?`,
    `ack?`, `cursor?`, `limit=50(1-200)`'
  response_shape: '** `{ items: EventItem[], next_cursor, has_more }`'
  response_header: '** `X-Events-Reconstruction`'
  front_client: '** `lib/api/events.ts` → `fetchEvents`'
db: '- events store (Timescale)'
back:
- already live (T-003 s04)
- URL search params ↔ `EventsQuery` mirror (deep links from StatusBar / Trends / Handoff)
- '**Не** внедрять QueryBuilder в этом eNN unless contract decision in CREATIVE —
  as-built inline params'
front:
- '`useEventsInfinite` + filters already wired (FRONT s10)'
- MSW must support multi-page cursor for E2E or E2E on live API
as_built:
- 'Contract: GET /api/events'
- 'FRONT client: lib/api/events.ts` → `fetchEvents'
- 'BACK: already live (T-003 s04)'
delta:
- 'Outcome: filter bar + infinite-scroll list of events'
- 'Исправить/заменить mock: MSW (static list; cursor poorly simulated)'
- 'BACK caveat: URL search params ↔ `EventsQuery` mirror (deep links from StatusBar
  / Trends / Handoff)'
- 'BACK caveat: Не** внедрять QueryBuilder в этом eNN unless contract decision in
  CREATIVE — as-built inline params'
- 'FRONT: MSW must support multi-page cursor for E2E or E2E on live API'
- 'Data need: cursor pagination; filters: from/to, event_name[], severity[], asset_id,
  source, ack?, limit'
- 'FRONT: MSW must support multi-page cursor for E2E or E2E on live API'
- 'BACK: URL search params ↔ `EventsQuery` mirror (deep links from StatusBar / Trends
  / Handoff)'
out_of_scope:
- Полный INTEG QA journey / соседние eNN — не этот шаг
- Не расширять scope за пределы contract + checkpoints
grep_control:
- back: GET /api/events
  front: fetchEvents
verify:
- rg "/api/events" frontend/src api/
- rg -n 'e11-v2' frontend/src apps || rg -n 'v2' frontend/src
- cd frontend && npm exec vitest -- run src/**/vitest `JournalPage.test.tsx
- rg -n 'v4' frontend/src frontend/e2e
tdd:
- '1. Red: vitest JournalPage with filters → assert list'
- '2. Green: live fetch + cursor, remove MSW'
- 3. Wire + §Verify
checkpoints:
- id: cp1
  criterion: /journal shows live events
  verify: rg "/api/events" frontend/src api/
- id: cp2
  criterion: Filters applied → URL + list match
  verify: rg -n 'e11-cp2' frontend/src apps || rg -n 'cp2' frontend/src
- id: cp3
  criterion: Infinite scroll loads next page
  verify: cd frontend && npm exec vitest -- run src/**/vitest JournalPage.test.tsx
