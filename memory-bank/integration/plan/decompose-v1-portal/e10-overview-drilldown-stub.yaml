schema: epic-decompose/v1
role: integ
step_id: e10
plan_id: v1-portal
title: Overview drill-down stub → future mnemo
element_id: e10
next_phase: INTEG IMPLEMENT
goal: 'copy «Мнемосхема: фаза 2» (stub, not functional mnemo screen)'
ui:
  route: '** /overview'
  components: '** `DrillDownStubModal`'
  user_sees: '** copy «Мнемосхема: фаза 2» (stub, not functional mnemo screen)'
data_need:
- 'future: schema list + values for selected group/system'
api_today:
- status: ❌
  detail: 'FRONT: stub only'
- status: ✅
  detail: 'BACK: `GET /api/mnemo/schemas`, `GET /api/mnemo/schemas/{id}`, `GET /api/mnemo/schemas/{id}/values`,
    `WS /api/mnemo/{schema_id}`'
contract:
  method_path: '** `GET /api/mnemo/schemas/{schema_id}/values`'
  front_client: '** (future) `lib/api/mnemo.ts` — currently absent'
db: '- mnemo registry (schemas + generators)'
back:
- already live (T-005 s08/s09)
- 'P1 wire: replace stub with screen/panel + client'
front:
- stub only (FRONT s09)
- 'До wire: оставить stub, но в DECOMPOSE пометить depends on FRONT creative mnemo'
as_built:
- frontend/src/features/overview drilldown stub (FRONT)
- Assets tree nodes clickable shell
delta:
- frontend/src/features/overview — wire stub navigate / placeholder detail
- No full asset detail API in this step
out_of_scope:
- Полный INTEG QA journey / соседние eNN — не этот шаг
- Не расширять scope за пределы contract + checkpoints
grep_control:
- back: GET /api/mnemo/* + WS
  front: (future client)
verify:
- rg "mnemo\|DrillDownStub" frontend/src api/
- rg -n 'cp2' frontend/src frontend/e2e
tdd:
- '1. Red: n/a (stub, no data call yet)'
- '2. Green: after FRONT creative mnemo — wire client + UI'
- 3. Wire + §Verify
checkpoints:
- id: cp1
  criterion: 'Stub visible on overview (copy "Мнемосхема: фаза 2")'
  verify: rg "mnemo\|DrillDownStub" frontend/src api/
- id: cp2
  criterion: 'After wire: click → real mnemo schema loads'
  verify: rg -n 'cp2' frontend/src frontend/e2e
