schema: epic-decompose/v1
role: integ
step_id: e27
plan_id: v1-portal
title: Vessel state
element_id: e27
next_phase: INTEG IMPLEMENT
goal: (future) mode ANCHORAGE/STEAMING/… with TTL
ui:
  route: '** none (ops/mode chip future)'
  components: '** (future) vessel mode chip (read-only first)'
  user_sees: '** (future) mode ANCHORAGE/STEAMING/… with TTL'
data_need:
- mode ANCHORAGE/STEAMING/…; override with TTL (permissioned)
api_today:
- status: ✅
  detail: '`GET /api/vessel/state`, `POST /api/vessel/state/override`'
- status: —
  detail: FRONT ❌
contract:
  method_path: '** `POST /api/vessel/state/override` body `{ mode, ttl_minutes }`'
  response_shape: '** `VesselStateResponse`'
  front_client: '** (future) `lib/api/vessel.ts`'
db: '- vessel svc (mode + override TTL)'
back:
- already live (T-005 s11)
- OTA gate depends on vessel mode (admin) — crew UI may be read-only chip first
- Permission matrix B11 for override
front:
- no UI yet
- OTA gate depends on vessel mode (admin) — crew UI may be read-only chip first
as_built:
- 'Contract: POST /api/vessel/state/override` body `{ mode, ttl_minutes }'
- 'FRONT client: (future) `lib/api/vessel.ts'
- 'BACK: already live (T-005 s11)'
delta:
- 'Outcome: (future) mode ANCHORAGE/STEAMING/… with TTL'
- 'BACK caveat: OTA gate depends on vessel mode (admin) — crew UI may be read-only
  chip first'
- 'BACK caveat: Permission matrix B11 for override'
- 'FRONT: no UI yet'
- 'FRONT: OTA gate depends on vessel mode (admin) — crew UI may be read-only chip
  first'
- 'Data need: mode ANCHORAGE/STEAMING/…; override with TTL (permissioned)'
- 'FRONT: no UI yet'
- 'FRONT: OTA gate depends on vessel mode (admin) — crew UI may be read-only chip
  first'
out_of_scope:
- Полный INTEG QA journey / соседние eNN — не этот шаг
- Не расширять scope за пределы contract + checkpoints
grep_control:
- back: GET /api/vessel/state
  front: (future client)
- back: POST /api/vessel/state/override
  front: (future client)
verify:
- rg "/api/vessel/state" frontend/src api/
- rg -n 'e27-v2' frontend/src apps || rg -n 'v2' frontend/src
- rg -n 'e27' frontend/e2e frontend/src
tdd:
- '1. Red: n/a (P1, no UI yet)'
- '2. Green: add client + read-only chip first'
- 3. Wire + §Verify
checkpoints:
- id: cp1
  criterion: Read-only chip shows current mode
  verify: rg "/api/vessel/state" frontend/src api/
- id: cp2
  criterion: Override (admin only) → clear error if denied
  verify: rg -n 'e27-cp2' frontend/src apps || rg -n 'cp2' frontend/src
- id: cp3
  criterion: OTA respects vessel mode gate
  verify: rg -n 'e27' frontend/e2e frontend/src
