schema: epic-decompose/v1
role: integ
step_id: e14
plan_id: v1-portal
title: Journal realtime append
element_id: e14
next_phase: INTEG IMPLEMENT
goal: new events appended to list in realtime (WS)
ui:
  route: '** /journal'
  components: '** `useEventsRealtime.ts`'
  user_sees: '** new events appended to list in realtime (WS)'
data_need:
- WS event frames merged into infinite query pages
api_today:
- status: ✅
  detail: WS events channel (e07)
- status: —
  detail: no dedicated REST
contract:
  front_client: '** `useEventsRealtime.ts`'
db: '- none (WS fanout from events store)'
back:
- WS events already live (e07, T-003 s06)
- Dedupe by event `id` when REST page + WS overlap
front:
- '`useEventsRealtime` already wired (FRONT s10)'
- Respect active filters client-side on WS append
as_built:
- 'FRONT client: useEventsRealtime.ts'
- 'BACK: WS events already live (e07, T-003 s06)'
delta:
- 'Outcome: new events appended to list in realtime (WS)'
- 'BACK caveat: Dedupe by event `id` when REST page + WS overlap'
- 'FRONT: Respect active filters client-side on WS append'
- 'Data need: WS event frames merged into infinite query pages'
- 'FRONT: Respect active filters client-side on WS append'
- 'BACK: Dedupe by event `id` when REST page + WS overlap'
out_of_scope:
- Полный INTEG QA journey / соседние eNN — не этот шаг
- Не расширять scope за пределы contract + checkpoints
grep_control:
- back: WS events channel (e07)
  front: useEventsRealtime
verify:
- rg -n "useEventsRealtime" frontend/src
- cd frontend && npm exec vitest -- run src/features/journal
- rg -n "events" frontend/src/hooks frontend/src/lib/ws
tdd:
- '1. Red: vitest MockWebSocket event → assert append'
- '2. Green: live WS, dedupe + filter respect'
- 3. Wire + §Verify
checkpoints:
- id: cp1
  criterion: New event via WS appears in list
  verify: rg -n "useEventsRealtime" frontend/src
- id: cp2
  criterion: Dedupe works (no duplicate rows)
  verify: cd frontend && npm exec vitest -- run src/features/journal
- id: cp3
  criterion: Filter active → WS append respects it
  verify: rg -n "events" frontend/src/hooks frontend/src/lib/ws
