schema: epic-decompose/v1
role: integ
step_id: e23
plan_id: v1-portal
title: Watch handoff buttons
element_id: e23
next_phase: INTEG IMPLEMENT
goal: buttons "Активные тревоги" / "Сейчас" → deep-link to journal
ui:
  route: '** /watch'
  components: '** `HandoffButton.tsx` + `lib/routing/handoff`'
  user_sees: '** buttons "Активные тревоги" / "Сейчас" → deep-link to journal'
data_need:
- client-only links to journal presets (active alarms / active now)
api_today:
- status: —
  detail: static routing (no API)
contract:
  front_client: '** `lib/routing/handoff` constants + `HandoffButton`'
db: '- none (client routing)'
back:
- n/a (client-only)
- 'Optional P1: bind to `GET /api/watch/schedule` for «current watch» timestamps instead
  of relative client now'
front:
- '`HandoffButton` already wired (FRONT s14)'
- Preset query must be valid EventsQuery (e11)
as_built:
- 'FRONT client: lib/routing/handoff` constants + `HandoffButton'
delta:
- 'Outcome: buttons "Активные тревоги" / "Сейчас" → deep-link to journal'
- 'BACK caveat: n/a (client-only)'
- 'BACK caveat: Optional P1: bind to `GET /api/watch/schedule` for «current watch»
  timestamps instead of relative client now'
- 'FRONT: Preset query must be valid EventsQuery (e11)'
- 'Data need: client-only links to journal presets (active alarms / active now)'
- 'FRONT: Preset query must be valid EventsQuery (e11)'
- 'BACK: n/a (client-only)'
- 'BACK: Optional P1: bind to `GET /api/watch/schedule` for «current watch» timestamps
  instead of relative client now'
out_of_scope:
- Полный INTEG QA journey / соседние eNN — не этот шаг
- Не расширять scope за пределы contract + checkpoints
grep_control:
- back: N/A (client routing)
  front: lib/routing/handoff
verify:
- rg "HandoffButton\|handoff" frontend/src
- cd frontend && npm exec vitest -- run src/features/watch
- rg -n 'cp3' frontend/src frontend/e2e
tdd:
- '1. Red: vitest handoff button → assert URL params'
- '2. Green: click → journal with correct filters'
- 3. Wire + §Verify
checkpoints:
- id: cp1
  criterion: Click "Активные тревоги" → journal with severity=alarm
  verify: rg "HandoffButton\|handoff" frontend/src
- id: cp2
  criterion: Click "Сейчас" → journal with from=now-ish
  verify: cd frontend && npm exec vitest -- run src/features/watch
- id: cp3
  criterion: Params match EventsQuery contract
  verify: rg -n 'cp3' frontend/src frontend/e2e
