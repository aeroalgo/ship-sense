schema: epic-decompose/v1
role: integ
step_id: e13
plan_id: v1-portal
title: Journal session event filter
element_id: e13
next_phase: INTEG IMPLEMENT
goal: toggle/filter for `source=session` events (login/logout)
ui:
  route: '** /journal'
  components: '** `SessionEventFilter.tsx`'
  user_sees: '** toggle/filter for `source=session` events (login/logout)'
data_need:
- filter `source=session` (and related session events from B11)
api_today:
- status: ✅
  detail: same `/api/events?source=session`
- status: —
  detail: FRONT filter util maps UI → query
contract:
  method_path: '** `GET /api/events?source=session&from=&to=&…`'
  query_keys: '** `source=session` + standard event filters'
  front_client: '** filter util in `journalFilters.ts`'
db: '- events store (source=session emitted by BACK session service)'
back:
- Session login/logout must emit events visible here (BACK session service)
- Empty state when no session events
front:
- '`SessionEventFilter` already exists (FRONT s10)'
- Filter util maps UI → query
as_built:
- 'Contract: GET /api/events?source=session&from=&to=&…'
- 'FRONT client: filter util in `journalFilters.ts'
delta:
- 'Outcome: toggle/filter for `source=session` events (login/logout)'
- 'BACK caveat: Session login/logout must emit events visible here (BACK session service)'
- 'BACK caveat: Empty state when no session events'
- 'FRONT: Filter util maps UI → query'
- 'Data need: filter `source=session` (and related session events from B11)'
- 'FRONT: `SessionEventFilter` already exists (FRONT s10)'
- 'FRONT: Filter util maps UI → query'
- 'BACK: Session login/logout must emit events visible here (BACK session service)'
out_of_scope:
- Полный INTEG QA journey / соседние eNN — не этот шаг
- Не расширять scope за пределы contract + checkpoints
grep_control:
- back: GET /api/events?source=session
  front: filter util
verify:
- rg "source=session\|SessionEventFilter" frontend/src api/
- rg -n 'cp2' frontend/src frontend/e2e
- rg -n "session_id|sessionId" frontend/src/features/journal frontend/src/lib/api
tdd:
- '1. Red: n/a (filter on existing endpoint)'
- '2. Green: login → emit event → filter shows it'
- 3. Wire + §Verify
checkpoints:
- id: cp1
  criterion: Login/logout emits session event
  verify: rg "source=session\|SessionEventFilter" frontend/src api/
- id: cp2
  criterion: Filter `source=session` shows only session events
  verify: rg -n 'cp2' frontend/src frontend/e2e
- id: cp3
  criterion: Empty state when no matches
  verify: rg -n "session_id|sessionId" frontend/src/features/journal frontend/src/lib/api
